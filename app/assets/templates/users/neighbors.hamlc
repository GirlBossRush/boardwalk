- directions = ['north', 'east', 'south', 'west']

- for neighbor, i in @user.get('neighbors')
  %aside.neighbor{id: directions[i]}
    %a{href: "/users/#{neighbor.user_id}", data: {username: neighbor.username}}

